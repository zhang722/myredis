#include "ZSet.h"

namespace myredis
{
bool ZSet::insert(RedisObjectPtr key, float val) {
    if (m_.count(key)) {
        
    }
}

bool ZSet::remove(RedisObjectPtr key)
{
return false;
}

    
} // namespace myredis
